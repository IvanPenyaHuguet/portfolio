import styled from '@emotion/styled';

const Header = styled.header(() => ({
  paddingTop: '200px'
}));

export default function HomePage() {
  return (
    <div>
      <Header>{'asd'}</Header>
    </div>
  );
}
